<form id="priorite_delete" method="post" action="{{ path('priorite_delete', {'id': priorite.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ priorite.id) }}">
</form>
