{{ form_start(form) }}
    <div class="form-group">
        {{ form_row(form.nom, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div class="form-group">
        {{ form_row(form.prenom, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div class="form-group">
        {{ form_row(form.mail, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div class="form-group">
        {{ form_row(form.password, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div class="form-group">
        {{ form_row(form.Commune, {'attr': {'class': 'form-control selectpicker'}}) }}
    </div>
    <div class="row">
        <div class="col">
            <button class="btn btn-success btn-lg btn-block">{{ button_label|default('Confirmer') }}</button>
        </div>
    </div>
{{ form_end(form) }}
{% if is_granted('ROLE_USER') %}
<a href={{ path("personne_editPassword") }}> Modifier mon mot de passe</a> <br>
{% endif %}