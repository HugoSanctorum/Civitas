{% extends 'base.html.twig' %}

{% block title %}Civitas Home{% endblock %}

{% block body %}

	{% for message in app.flashes('fail') %}
		<div class="alert alert-danger">
			{{ message }}
		</div>
	{% endfor %}
	<style type="text/css">
		.header-image
		{
			background-image: url( {{ asset("/images/lens.jpg") }} );
		}
	</style>
	<div class="main_container mb-4">
		<div class="header-image"></div>
		<div class="container header">
			<div class="text-center">
				<h1 class="display-4 text-primary">CIVITAS {{commune|upper}}</h1>
			</div>
		</div>
	</div>
	<div class="row text-center">
      	<div class="col-lg-4 mb-5">
			<span class="fa-stack fa-4x">
				<i class="fa fa-circle fa-stack-2x text-info"></i>
				<i class="fa fa-globe-europe fa-stack-1x text-light"></i>
			</span>
			<h2>Carte</h2>
			<p>Vous pouvez visualiser les problèmes de {{commune}} sur une carte.</p>
			<p><a class="btn btn-secondary" href="{{ path('home_carte') }}" role="button">Voir <i class="fa fa-arrow-right"></i></a></p>
      	</div>
      	<div class="col-lg-4 mb-5">
      		<span class="fa-stack fa-4x">
				<i class="fa fa-circle fa-stack-2x text-warning"></i>
				<i class="fa fa-user-circle fa-stack-1x text-light"></i>
			</span>
	        <h2>Profil</h2>
	        <p>Vous pouvez gérer vos problèmes et voir leurs statuts.</p>
	        <p><a class="btn btn-secondary" href="{{ path('personne_signalements') }}" role="button">Gérer <i class="fa fa-arrow-right"></i></a></p>
      	</div>
      	<div class="col-lg-4 mb-5">
	        <span class="fa-stack fa-4x">
				<i class="fa fa-circle fa-stack-2x text-success"></i>
				<i class="fa fa-exclamation-circle fa-stack-1x text-light"></i>
			</span>
	        <h2>Signaler</h2>
	        <p>Vous pouvez signaler un problème.</p>
	        <p><a class="btn btn-secondary" href="{{ path('probleme_new') }}" role="button">Signaler <i class="fa fa-arrow-right"></i></a></p>
      	</div>
    </div>
{% endblock %}