{% extends 'base.html.twig' %}

{% block title %}CompteRendu{% endblock %}

{% block body %}
    <h1>CompteRendu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compte_rendu.id }}</td>
            </tr>
            <tr>
                <th>UrlDocument</th>
                <td>{{ compte_rendu.urlDocument }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('compte_rendu_index') }}">back to list</a>

    <a href="{{ path('compte_rendu_edit', {'id': compte_rendu.id}) }}">edit</a>

    {{ include('compte_rendu/_delete_form.html.twig') }}
{% endblock %}
