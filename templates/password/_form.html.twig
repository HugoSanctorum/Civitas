{{ form_start(form) }}
    <div class="form-group">
        {{ form_row(form.oldPassword, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div class="form-group">
        {{ form_row(form.newPassword, {'attr': {'class': 'form-control'}}) }}
    </div>
<div class="row">
    <div class="col">
        <a class="btn btn-primary btn-lg btn-block" href="{{ path('personne_profile') }}">Retour</a>
    </div>
    <div class="col">
        <button class="btn btn-success btn-lg btn-block">{{ button_label|default('Confirmer') }}</button>
    </div>
</div>
{{ form_end(form) }}